<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baitap.sin.quanli">

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/hinhlogo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".quanlithuchi"
            android:theme="@style/Theme.Design.Light.NoActionBar" />
        <activity
            android:name=".themgiaodich_activity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Viewgd_activity"
            android:parentActivityName=".themgiaodich_activity" />
        <activity
            android:name=".Main2Activity"
            android:theme="@style/Base.Theme.AppCompat.Light.Dialog" />
        <activity android:name=".Bieudo_Activity"
            android:parentActivityName=".quanlithuchi"></activity>
    </application>

</manifest>